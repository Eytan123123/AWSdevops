# Outputs exposed by the EC2 module.
# Currently empty — no value is consumed by other modules or required by the project spec.
# Add outputs here if a future caller needs to read attributes (e.g., the ASG name).
